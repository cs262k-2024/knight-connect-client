/**
 * @file This file contains the layout component for the login page.
 */

import { Slot } from 'expo-router';

/**
 * LoginLayout component renders a Slot component which acts as a placeholder for nested routes.
 *
 * @returns {JSX.Element} The rendered Slot component.
 */
export default function LoginLayout() {
    return <Slot />;
}
